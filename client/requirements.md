## Packages
framer-motion | Smooth animations for a premium feel
lucide-react | Beautiful icons (already in base but confirming usage)
date-fns | Formatting dates for admin table

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
